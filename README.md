# Firefox new tab map

1. Clone repo
1. Add your map to `img/map.png`.
1. Navigate in Firefox to `about:debugging` and click `Load Temporary Add-On`
1. Choose the `manifest.json` from this repo
1. Open a new tab!
